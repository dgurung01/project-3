//- views/layout.pug

extends layout

block content
br.w3-container.w3-card-4
header
h1 Welcome  #{user.nickname}
img(src=user.picture)
h2 user profile
p This is the content of <code> req.user</code>
p Not: <code>_raw</code> and <code>_json</code> properties have been ommited
pre
    code #{userProfile}